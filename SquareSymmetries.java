package geometry;


import java.util.*;
import java.util.Collection;

/*public class SquareSymmetries implements Symmetries<Square> {
    @Override
    public boolean areSymmetric(Shape s1, Shape s2) {
        return false;
    }

    @Override
    public Collection symmetriesOf(Shape shape) {
        List<Square> sym=new ArrayList<>();
        Square s=(Square)shape;
        return sym;
    }
    private void horizontalMirror (Square s, List<Square> list)
    {
       List<Point> temp=s.getSides();
       Point first=new Point(
           Point second=new Point(
        list.add(sq);
    }
}*/
